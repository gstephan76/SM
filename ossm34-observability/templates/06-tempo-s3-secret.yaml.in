apiVersion: v1
kind: Secret
metadata:
  name: tempo-s3
  namespace: tempo
type: Opaque
stringData:
  bucket: "${S3_BUCKET}"
  endpoint: "${S3_ENDPOINT}"
  access_key_id: "${S3_ACCESS_KEY_ID}"
  access_key_secret: "${S3_ACCESS_KEY_SECRET}"
